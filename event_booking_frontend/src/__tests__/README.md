# Frontend Test Suite

- Run: `npm test` or `npx jest --config jest.config.js src/__tests__/`
- All component, hook, and page tests live in this folder.
- Uses React Testing Library, jsdom environment. All API calls are mocked.
